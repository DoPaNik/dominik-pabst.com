---
import security from '../../assets/illustrations/security.svg?raw';
import chat from '../../assets/illustrations/chat.svg?raw';

const ILLUSTRATIONS = { security, chat };

interface Props {
  name: keyof typeof ILLUSTRATIONS;
  tone?: 'accent' | 'photo' | 'amber' | 'ink';
  class?: string;
  decorative?: boolean;
  label?: string;
}

const { name, tone = 'ink', class: className = '', decorative = false, label } = Astro.props;
const svg = ILLUSTRATIONS[name];
const cls = ['dpn-illo', tone ? `dpn-illo--${tone}` : '', className].filter(Boolean).join(' ');
---

<span
  class={cls}
  set:html={svg}
  role={decorative ? 'presentation' : 'img'}
  aria-hidden={decorative ? 'true' : undefined}
  aria-label={!decorative ? label : undefined}
/>

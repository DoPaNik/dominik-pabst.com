---
import '../../styles/components/role-rotator.css';

interface Props {
  roles: string[];
  class?: string;
}

const { roles, class: className = '' } = Astro.props;
---

<span
  class={['dpn-role-rotator', className].filter(Boolean).join(' ')}
  data-roles={JSON.stringify(roles)}
  aria-hidden="true">{roles[0]}</span
><span class="dpn-role-rotator__cursor" aria-hidden="true"></span><span class="visually-hidden"
  >{roles.join(', ')}</span
>

<script is:inline src="/scripts/role-rotator.js"></script>
